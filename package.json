{
  "name": "fox-homepage-mk1",
  "private": true,
  "packageManager": "npm@11.7.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "deploy": "npm run deploy:api && npm run deploy:web",
    "deploy:api": "cd apps/api && npm run deploy",
    "deploy:web": "cd apps/web && npm run deploy",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
